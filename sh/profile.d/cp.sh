cp() { command cp -aiv "$@"; }
#                  ││└─ verbose
#                  │└── interactive
#                  └─── archive mode (recursive; don't follow symlinks; 
#                       preserve attributes)
