#!/usr/bin/env bash

# Placing this script in $PATH will enable the command `brew env`, which lists
# all `HOMEBREW_*` environment variables visible to Homebrew while it's running.

env | grep '^HOMEBREW_' | sort
