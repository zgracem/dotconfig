# ----------------------------------------------------------------------------
# Ruby
# ----------------------------------------------------------------------------

include ../common.mk

Gemfile.lock: Gemfile
	gem install --file=$< --lock
install: Gemfile.lock
