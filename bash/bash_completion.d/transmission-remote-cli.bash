# transmission-remote-cli
# >> https://github.com/fagga/transmission-remote-cli
f="$HOME/opt/etc/bash_completion.d/transmission-remote-cli-bash-completion.sh"
# shellcheck disable=SC1090
[[ -f $f ]] && . "$f"
unset -v f
